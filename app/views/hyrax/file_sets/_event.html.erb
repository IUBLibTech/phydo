<tr class="attributes">
  <td class="attribute"><%= link_to(event.try(:type), event.try(:link)) %></td>
  <td class="attribute"><%= event.try(:outcome) %></td>
  <td class="attribute"><%= event.try(:date) %></td>
  <td class="attribute"><%= event.try(:agent) %></td>
</tr>

